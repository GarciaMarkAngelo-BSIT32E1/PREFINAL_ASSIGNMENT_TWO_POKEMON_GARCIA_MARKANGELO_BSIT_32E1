@model PokemonCatcher.Models.Pokemon

<h1>@Model.Name Details</h1>

<h2>Moves</h2>
<ul>
    @foreach (var move in Model.Moves)
    {
        <li>@move.Move.Name</li>
    }
</ul>

<h2>Abilities</h2>
<ul>
    @foreach (var ability in Model.Abilities)
    {
        <li>@ability.Ability.Name</li>
    }
</ul>
